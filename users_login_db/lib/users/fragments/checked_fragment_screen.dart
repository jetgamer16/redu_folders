import 'package:flutter/material.dart';

class MadeFragmentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Checked Fragment Screen"),
      ),
    );
  }
}
